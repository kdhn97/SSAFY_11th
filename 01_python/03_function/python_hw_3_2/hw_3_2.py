# 아래 함수를 수정하시오.
# 각 매개변수는 위치 인자들에 맞춰서 값이 할당이된다.
def add_numbers(num1, num2):
    # 습관 들이기
    # 최종 결과물 할당할 변수
    result = num1 + num2
    # 최종 결과물 반환
    return result


# 수정한 add_numbers() 함수를 호출하시오.
result = add_numbers(3, 6)
print(result)
print(add_numbers(2, 4))
